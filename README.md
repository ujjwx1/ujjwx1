```
```
           _   _              ___
  __  __  (_) (_)      ___  _<  /
 / / / / / / / / | /| / / |/_/ / 
/ /_/ / / / / /| |/ |/ />  </ /  
\__,_/_/ /_/ / |__/|__/_/|_/_/   
    /___/___/                                     
```

```
# ujjwal aggarwal  ·  ujjwx1
# thapar institute  ·  patiala, india
# ml / data / systems
```

---

**❯ whoami**

engineer who builds things that learn. i care more about the parts that break quietly than the parts that demo well — pipelines, failure modes, infrastructure. if a system works at 3am without me watching it, that's the goal.

---

**❯ ls ./open-source**

`dspinellis/cscout` · C++ · *contributor*
> refactoring browser for C codebases. static analysis at scale. the kind of tooling most engineers treat as someone else's problem — i opened it up.

`docker-ml-system` · python · *author*
> containerized ml stack — real-time inference API, persistent storage, monitoring. orchestrated with compose. exists because "it works on my machine" is not a deployment strategy.

`variable-geometry-robot` · embedded · *author*
> autonomous surveillance robot with adaptive geometry. CAD + PCB + docs. hardware is just another system with worse stack traces.

---

**❯ cat ./now.txt**

```
→ studying how models fail, not just how they perform
→ cs @ thapar — applied ml track
→ contributing to open source when i find something worth fixing
```

---

**❯ env | grep STACK**

`python` `pytorch` `docker` `terraform` `linux` `c++` `bash`

---

`// no noise. just commits.`                
```

```
# ujjwal aggarwal  ·  ujjwx1
# thapar institute  ·  patiala, india
# ml / data / systems
```

---

**❯ whoami**

engineer who builds things that learn. i care more about the parts that break quietly than the parts that demo well — pipelines, failure modes, infrastructure. if a system works at 3am without me watching it, that's the goal.

---

**❯ ls ./open-source**

`dspinellis/cscout` · C++ · *contributor*
> refactoring browser for C codebases. static analysis at scale. the kind of tooling most engineers treat as someone else's problem — i opened it up.

`docker-ml-system` · python · *author*
> containerized ml stack — real-time inference API, persistent storage, monitoring. orchestrated with compose. exists because "it works on my machine" is not a deployment strategy.

`variable-geometry-robot` · embedded · *author*
> autonomous surveillance robot with adaptive geometry. CAD + PCB + docs. hardware is just another system with worse stack traces.

---

**❯ cat ./now.txt**

```
→ studying how models fail, not just how they perform
→ cs @ thapar — applied ml track
→ contributing to open source when i find something worth fixing
```

---

**❯ env | grep STACK**

`python` `pytorch` `docker` `terraform` `linux` `c++` `bash`

---

`// no noise. just commits.`
